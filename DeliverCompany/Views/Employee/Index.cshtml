@* @model IEnumerable<DeliverCompany.Models.Employee>

<h2>Employees</h2>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var employee in Model)
        {
            <tr>
                <td>@employee.Name</td>
                <td>@employee.Email</td>
                <td>
                    <a href="/Employee/Edit/@employee.Id" class="btn btn-primary">Edit</a>
                    <a href="/Employee/Delete/@employee.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a href="/Employee/Create" class="btn btn-success">Add New Employee</a>
 *@