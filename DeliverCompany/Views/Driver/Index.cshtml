@model IEnumerable<Driver>

@* <style>
    body {
        background-color: #f0f8ff; /* Ljusblå bakgrund för att skapa ett mjukt intryck */
    }

    .container {
        background-color: #ffffff; /* Vit bakgrund för innehållsbehållare */
        border-radius: 15px; /* Rundade hörn för behållaren */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Liten skugga för en lyft effekt */
        padding: 30px;
    }

    h2 {
        color: #333333;
        font-weight: bold;
    }

    .form-control {
        border-radius: 10px; /* Rundade hörn för formulärfält */
        border: 1px solid #dcdcdc;
    }

    .btn-primary {
        background-color: #007bff; /* Bootstrap primärblå */
        border-radius: 20px; /* Rundade hörn för knappar */
        border: none;
        padding: 10px 20px;
    }

    .btn-info, .btn-warning, .btn-danger {
        border-radius: 20px; /* Rundade hörn för action-knappar */
        padding: 8px 15px;
    }

    .btn-info {
        background-color: #17a2b8;
        border: none;
    }

    .btn-warning {
        background-color: #ffc107;
        border: none;
    }

    .btn-danger {
        background-color: #dc3545;
        border: none;
    }

    .table {
        border-radius: 10px;
        overflow: hidden; /* För att undvika att innehåll flyter över */
    }

        .table thead {
            background-color: #007bff; /* Mörkblå rubrik */
            color: white;
        }

        .table tbody tr:hover {
            background-color: #f1f1f1; /* Lätt grå färg när man hovrar över tabellrader */
        }

        .table td, .table th {
            vertical-align: middle; /* Snyggare vertikal justering */
        }
</style> *@


    <h2 class="mb-4 text-center">Driver List</h2>

    <!-- Search form -->
    <div class="search-bar d-flex justify-content-center mb-4">
        <form class="form-inline" method="get">
            <div class="form-group mr-2">
                <input type="text"
                       class="form-control"
                       placeholder="Search by name..."
                       name="searchString"
                       value="@ViewContext.HttpContext.Request.Query["searchString"]" />
            </div>
            <div class="form-group mr-2">
                <label for="fromNoteDate" class="sr-only">From</label>
                <input type="date"
                       class="form-control"
                       name="fromNoteDate"
                       value="@ViewContext.HttpContext.Request.Query["fromNoteDate"]" />
            </div>
            <div class="form-group mr-2">
                <label for="toNoteDate" class="sr-only">To</label>
                <input type="date"
                       class="form-control"
                       name="toNoteDate"
                       value="@ViewContext.HttpContext.Request.Query["toNoteDate"]" />
            </div>
            <button type="submit" class="btn btn-primary">
                <i class="fas fa-search"></i> Search
            </button>
        </form>
    </div>

    <!-- Table to display driver data -->
    <div class="table-responsive">
        <table class="table table-hover table-bordered">
            <thead class="thead">
                <tr>
                    <th>Driver Name</th>
                    <th>Car Registration</th>
                    <th>Note Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var driver in Model)
                {
                    <tr>
                        <td>@driver.DriverName</td>
                        <td>@driver.CarReg</td>
                        <td>@driver.NoteDate.ToShortDateString()</td>
                        <td>
                            <a href="@Url.Action("Details", new { id = driver.DriverID })" class="btn btn-sm btn-info">
                                <i class="fas fa-info-circle"></i> Details
                            </a>
                            <a href="@Url.Action("Edit", new { id = driver.DriverID })" class="btn btn-sm btn-warning">
                                <i class="fas fa-edit"></i> Edit
                            </a>
                            <a href="@Url.Action("Delete", new { id = driver.DriverID })" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this driver?');">
                                <i class="fas fa-trash-alt"></i> Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
